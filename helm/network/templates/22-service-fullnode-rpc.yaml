apiVersion: v1
kind: Service
metadata:
  name: sui-fullnode-rpc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: sui-fullnode
  ports:
    - name: rpc
      port: {{ .Values.fullnode.port_rpc }}
      targetPort: {{ .Values.fullnode.port_rpc }}
  type: ClusterIP
